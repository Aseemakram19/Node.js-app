pipeline {
    agent any

    stages {             
        stage('Stage-2 , Deploy') {
            steps {
               sh 'echo "deploying application..."'
            }               
        }
    }
}
